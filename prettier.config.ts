import type { Config } from "prettier";

const config: Config = {
	trailingComma: "all",
	tabWidth: 4,
	useTabs: true,
	semi: true,
	singleQuote: false,
	experimentalTernaries: true,
	printWidth: 170,
	arrowParens: "avoid",
};

export default config;
